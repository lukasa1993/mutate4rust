name: Finalize review fixes

on:
  push:
    branches: [scope-correctness-v204]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  finalize:
    runs-on: ubuntu-latest
    timeout-minutes: 35
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v7
        with:
          ref: scope-correctness-v204
          fetch-depth: 0
      - name: Install stable Rust
        run: |
          rustup toolchain install stable --profile minimal --component rustfmt,clippy
          rustup default stable
      - name: Apply native Rust review fixes
        run: |
          mkdir -p target
          rustc tools/finalize_review.rs -o target/finalize-review
          target/finalize-review
          cargo fmt --all
      - name: Verify final source
        run: |
          cargo metadata --locked --format-version 1 --no-deps > $RUNNER_TEMP/metadata.json
          cargo clippy --locked --all-targets --all-features -- -D warnings
          cargo test --locked --all-targets --all-features
          cargo build --locked --release
          cargo install --path . --locked --root target/native-install --force
          target/native-install/bin/mutate4rust --version
          target/native-install/bin/mutate4rust --help >/dev/null
      - name: Publish reviewed source and restore strict CI
        shell: bash
        run: |
          git fetch origin main scope-correctness-v204
          git show origin/main:.github/workflows/ci.yml > .github/workflows/ci.yml
          rm tools/finalize_review.rs
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add .github/workflows/ci.yml src/lib.rs src/scope.rs tools/finalize_review.rs
          if git diff --cached --quiet; then
            echo "No finalization changes are required."
            exit 0
          fi
          git commit -m "Finish Cargo-aware mutation scope hardening"
          git rebase origin/scope-correctness-v204
          git push origin HEAD:scope-correctness-v204
