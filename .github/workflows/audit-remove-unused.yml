name: Remove unused audit helper

on:
  push:
    branches: [audit-test-cfg-scope-20260823]

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          ref: audit-test-cfg-scope-20260823
      - name: Apply repair with native Rust
        shell: bash
        run: |
          cat > "$RUNNER_TEMP/patch.rs" <<'RS'
          use std::fs;
          fn main() {
              let path = "src/scope.rs";
              let mut text = fs::read_to_string(path).unwrap();
              let start_marker = "\n    pub(crate) fn single_variants(&self) -> Vec<Self> {";
              let start = text.find(start_marker).expect("single_variants start");
              let tail = &text[start..];
              let end_offset = tail
                  .find("\n}\n\nfn contexts_for_tests")
                  .expect("CfgContext impl end");
              text.replace_range(start..start + end_offset, "");
              let old = "assert_eq!(context.single_variants().len(), 2);";
              assert_eq!(text.matches(old).count(), 1);
              text = text.replacen(old, "assert_eq!(context.variants.len(), 2);", 1);
              fs::write(path, text).unwrap();
          }
          RS
