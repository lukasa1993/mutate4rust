# mutate4rust

Install with `cargo install --git https://github.com/lukasa1993/mutate4rust --force`. Run the real workspace tests and `cargo check` for validation. Never count timeout, invalid syntax, or compile errors as killed mutants. Exit `2` means survivors remain.
